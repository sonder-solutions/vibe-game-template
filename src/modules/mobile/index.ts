/**
 * Mobile Module
 * Provides mobile gesture protection and touch handling
 *
 * Optional module - can be tree-shaken if not used
 */

export { MobileProtection } from './MobileProtection';
export type { IMobileProtection, MobileProtectionConfig } from './MobileProtection.types';
